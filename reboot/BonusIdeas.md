# Bonus ideas

Things I can prove/define but that are not critical for the main story line.

* Embedding SIgs-with-named-external-args into SIgs-with-ℕ-args
    * And showing it preserves overlying term algebras up to set isomorphism.
* Adjunction Signatures-with-named-external-args and Sigs-with-ℕ-args.
* `NFLeq` and `NFFix` can also be equivalently defined via an `AllRestr`
  satisfaction of a `LocProp`. (they "are localisible").
